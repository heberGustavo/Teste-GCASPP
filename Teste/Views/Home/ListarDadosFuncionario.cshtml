@{
    Layout = "";
    @model IEnumerable<Teste.Domain.Models.EntityDomain.Funcionario>
}

<table class="table">
    <thead>
        <tr>
            <th scope="col">Nome</th>
            <th scope="col">Data de Nas.</th>
            <th scope="col">Salario</th>
            <th scope="col">Filho(s)</th>
            <th scope="col"></th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count() > 0)
        {
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.nome</td>
                    <td>@item.data_de_nascimento.ToString("dd/MM/yyyy")</td>
                    <td>@item.salario.ToString("C")</td>
                    <td>
                        <button onclick="AbrirModalFilhos(@item.id)" class="btn btn-sm btn-outline-warning">Filhos</button>
                    </td>
                    <td>
                        <button onclick="EditarFuncionario(@item.id)" class="btn btn-sm btn-warning btn-sm"><span class="material-icons">edit</span></button>
                        <button class="btn btn-sm btn-danger btn-sm"><span class="material-icons">delete</span></button>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td>Nenhum registro encontrado</td>
            </tr>
        }
    </tbody>
</table>